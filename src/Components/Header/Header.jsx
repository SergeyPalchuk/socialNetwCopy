import React from 'react'
import hd from './Header.module.css'

const Header = (props)=>{
    return(
        <div className={hd.header}>
            Header
        </div>
    )
}

export default Header;