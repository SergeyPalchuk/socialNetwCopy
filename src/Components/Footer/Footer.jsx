import React from 'react'
import ft from './Footer.module.css'

const Footer = (props)=>{
    return(
        <div className={ft.footer}>
            footer
        </div>
    )
}

export default Footer;